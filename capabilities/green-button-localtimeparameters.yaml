naftiko: 1.0.0-alpha2
info:
  label: Xcel Energy Green Button Connect My Data API — LocalTimeParameters
  description: 'Xcel Energy Green Button Connect My Data API — LocalTimeParameters. 1 operations. Lead operation: Retrieve
    LocalTimeParameters. Self-contained Naftiko capability covering one Xcel Energy business surface.'
  tags:
  - Xcel Energy
  - LocalTimeParameters
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    XCEL_ENERGY_API_KEY: XCEL_ENERGY_API_KEY
capability:
  consumes:
  - type: http
    namespace: green-button-localtimeparameters
    baseUri: https://api.xcelenergy.com/DataCustodian/espi/1_1/resource
    description: Xcel Energy Green Button Connect My Data API — LocalTimeParameters business capability. Self-contained, no
      shared references.
    resources:
    - name: LocalTimeParameters-localTimeParametersId
      path: /LocalTimeParameters/{localTimeParametersId}
      operations:
      - name: getlocaltimeparameters
        method: GET
        description: Retrieve LocalTimeParameters
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.XCEL_ENERGY_API_KEY}}'
  exposes:
  - type: rest
    namespace: green-button-localtimeparameters-rest
    port: 8080
    description: REST adapter for Xcel Energy Green Button Connect My Data API — LocalTimeParameters. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/localtimeparameters/{localtimeparametersid}
      name: localtimeparameters-localtimeparametersid
      description: REST surface for LocalTimeParameters-localTimeParametersId.
      operations:
      - method: GET
        name: getlocaltimeparameters
        description: Retrieve LocalTimeParameters
        call: green-button-localtimeparameters.getlocaltimeparameters
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: green-button-localtimeparameters-mcp
    port: 9090
    transport: http
    description: MCP adapter for Xcel Energy Green Button Connect My Data API — LocalTimeParameters. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: retrieve-localtimeparameters
      description: Retrieve LocalTimeParameters
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: green-button-localtimeparameters.getlocaltimeparameters
      outputParameters:
      - type: object
        mapping: $.
